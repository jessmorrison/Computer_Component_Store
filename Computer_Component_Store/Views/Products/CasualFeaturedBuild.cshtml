@model IEnumerable<Computer_Component_Store.Data.ComputerComponentProduct>

@{
    ViewData["Title"] = "CasualFeaturedBuild";
}

<h2>Featured Build - Casual</h2>
<div class="btn-text-divider"></div>


@* Casual Form *@
<form asp-action="index" method="post" id="CasualPicker">
    <div class="row">
        <div class="col">
            <div class="card">
                <div class="card-body">
                    @foreach (var product in Model.Where(x => x.ID == 1004))
                    {
                        <h2><a href="/products/index/@product.ID">@product.Name</a></h2>
                        <a href="/products/index/@product.ID"><img class="img-fluid" src="@Url.Content(product.ImageURL)" /></a>
                        <p>@product.Description</p>
                        <p>$@product.Price</p>
                        <input type="hidden" name="CaseIDCasual" value="@product.ID" />
                        <label for="quantity">How many would you like?</label>
                        <select name="quantity" class="form-control">
                            @for (int i = 1; i <= 10; i++)
                            {
                                <option value="@i">@i</option>
                            }
                        </select>
                    }
                </div>
            </div>
        </div>
        <div class="col">
            <div class="card">
                <div class="card-body">
                    @foreach (var product in Model.Where(x => x.ID == 2))
                    {
                        <h2><a href="/products/index/@product.ID">@product.Name</a></h2>
                        <a href="/products/index/@product.ID"><img class="img-fluid" src="@Url.Content(product.ImageURL)" /></a>
                        <p>@product.Description</p>
                        <p>$@product.Price</p>
                        <input type="hidden" name="MotherboardIDCasual" value="@product.ID" />
                        <label for="quantity">How many would you like?</label>
                        <select name="quantity" class="form-control">
                            @for (int i = 1; i <= 10; i++)
                            {
                                <option value="@i">@i</option>
                            }
                        </select>
                    }
                </div>
            </div>
        </div>
        <div class="col">
            <div class="card">
                <div class="card-body">
                    @foreach (var product in Model.Where(x => x.ID == 12))
                    {
                        <h2><a href="/products/index/@product.ID">@product.Name</a></h2>
                        <a href="/products/index/@product.ID"><img class="img-fluid" src="@Url.Content(product.ImageURL)" /></a>
                        <p>@product.Description</p>
                        <p>$@product.Price</p>
                        <input type="hidden" name="VideoCardIDCasual" value="@product.ID" />
                        <label for="quantity">How many would you like?</label>
                        <select name="quantity" class="form-control">
                            @for (int i = 1; i <= 10; i++)
                            {
                                <option value="@i">@i</option>
                            }
                        </select>
                    }
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col">
            <div class="card">
                <div class="card-body">
                    @foreach (var product in Model.Where(x => x.ID == 22))
                    {
                        <h2><a href="/products/index/@product.ID">@product.Name</a></h2>
                        <a href="/products/index/@product.ID"><img class="img-fluid" src="@Url.Content(product.ImageURL)" /></a>
                        <p>@product.Description</p>
                        <p>$@product.Price</p>
                        <input type="hidden" name="ProcessorIDCasual" value="@product.ID" />
                        <label for="quantity">How many would you like?</label>
                        <select name="quantity" class="form-control">
                            @for (int i = 1; i <= 10; i++)
                            {
                                <option value="@i">@i</option>
                            }
                        </select>
                    }
                </div>
            </div>
        </div>
        <div class="col">
            <div class="card">
                <div class="card-body">
                    @foreach (var product in Model.Where(x => x.ID == 32))
                    {
                        <h2><a href="/products/index/@product.ID">@product.Name</a></h2>
                        <a href="/products/index/@product.ID"><img class="img-fluid" src="@Url.Content(product.ImageURL)" /></a>
                        <p>@product.Description</p>
                        <p>$@product.Price</p>
                        <input type="hidden" name="RAMIDCasual" value="@product.ID" />
                        <label for="quantity">How many would you like?</label>
                        <select name="quantity" class="form-control">
                            @for (int i = 1; i <= 10; i++)
                            {
                                <option value="@i">@i</option>
                            }
                        </select>
                    }
                </div>
            </div>
        </div>
        <div class="col">
            <div class="card">
                <div class="card-body">
                    @foreach (var product in Model.Where(x => x.ID == 52))
                    {
                        <h2><a href="/products/index/@product.ID">@product.Name</a></h2>
                        <a href="/products/index/@product.ID"><img class="img-fluid" src="@Url.Content(product.ImageURL)" /></a>
                        <p>@product.Description</p>
                        <p>$@product.Price</p>
                        <input type="hidden" name="CoolingSystemIDCasual" value="@product.ID" />
                        <label for="quantity">How many would you like?</label>
                        <select name="quantity" class="form-control">
                            @for (int i = 1; i <= 10; i++)
                            {
                                <option value="@i">@i</option>
                            }
                        </select>
                    }
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-sm-4">
            <div class="card">
                <div class="card-body">
                    @foreach (var product in Model.Where(x => x.ID == 42))
                    {
                        <h2><a href="/products/index/@product.ID">@product.Name</a></h2>
                        <a href="/products/index/@product.ID"><img class="img-fluid" src="@Url.Content(product.ImageURL)" /></a>
                        <p>@product.Description</p>
                        <p>$@product.Price</p>
                        <input type="hidden" name="StorageIDCasual" value="@product.ID" />
                        <label for="quantity">How many would you like?</label>
                        <select name="quantity" class="form-control">
                            @for (int i = 1; i <= 10; i++)
                            {
                                <option value="@i">@i</option>
                            }
                        </select>
                    }
                </div>
            </div>
        </div>
    </div>
    <input type="submit" class="btn btn-danger" id="CasualSubmitButton" value="Add To Cart" style="width:100%; background-color:#9b333f;" />
</form>